spring:
  profiles:
    active: test
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.SQL: debug
  slack:
      webhook-uri: https://hooks.slack.com/services/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
      config: classpath:logback-spring.xml